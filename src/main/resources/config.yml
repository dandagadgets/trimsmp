# TrimSMP configuration
# A player's power activates only when all 4 armor pieces (helmet, chestplate,
# leggings, boots) carry the SAME trim pattern. The trim MATERIAL of the
# weakest piece in the set decides the power's tier (1-5, see tiers below).
#
# Tiers: 1=Common (iron/copper/quartz)  2=Uncommon (gold/redstone/lapis)
#        3=Rare (amethyst/emerald)      4=Epic (diamond)   5=Legendary (netherite)
#
# Every trim has a PASSIVE (always on while the set is worn) and an ACTIVE power,
# triggered by sneaking + swapping hands (default F key), on a per-pattern cooldown
# that shortens at higher tiers. Silence, Vex, Wild and Raiser/Ward also have extra
# mechanics that trigger automatically (a "disrupt") when you're badly hurt.
#
# Most numeric values below scale with tier automatically inside the code
# (base + tier * per-tier); the numbers here are the tunable base/step inputs.

general:
  # Seconds between passive ability ticks (potion effects, auras, scans).
  tick-interval-seconds: 1
  # Broadcast an action-bar hint the first time a player completes a matching set.
  announce-activation: true
  activation-message: '&d&lTRIM SET ACTIVE &7- &f%pattern% &7(tier %tier%)'

abilities:
  sentry:
    enabled: true
    radius: 15.0
    arrow-count: 3
    true-damage-base: 0.5
    true-damage-per-tier: 0.4
    cooldown-seconds-base: 90
    cooldown-seconds-reduction-per-tier: 10

  vex:
    enabled: true
    radius: 30.0
    damage-base: 8.0
    damage-per-tier: 1.0
    debuff-duration-seconds: 20
    blindness-duration-seconds: 5
    minion-count-base: 3
    minion-lifespan-seconds: 45
    cooldown-seconds-base: 120
    cooldown-seconds-reduction-per-tier: 12
    disrupt-health-threshold: 8.0
    disrupt-cooldown-seconds: 120
    disrupt-hide-duration-seconds: 10

  wild:
    enabled: true
    grapple-range: 60.0
    grapple-speed: 1.8
    poison-duration-seconds: 10
    grass-patch-radius: 2.5
    grass-patch-damage-base: 1.0
    grass-patch-damage-per-tier: 0.2
    grass-patch-duration-seconds: 4
    grass-patch-tick-interval: 10
    cooldown-seconds-base: 20
    cooldown-seconds-reduction-per-tier: 2
    root-trigger-health: 8.0
    root-cooldown-seconds: 20
    root-radius-xz: 5.0
    root-radius-y: 3.0
    root-duration-seconds: 10

  coast:
    enabled: true
    radius: 10.0
    radius-per-tier: 1.0
    damage-base: 6.0
    damage-per-tier: 1.0
    debuff-duration-seconds: 4
    pulse-count: 3
    pulse-interval-ticks: 8
    cooldown-seconds-base: 60
    cooldown-seconds-reduction-per-tier: 6

  dune:
    enabled: true
    radius: 12.0
    damage-base: 10.0
    damage-per-tier: 1.0
    duration-seconds: 10
    cooldown-seconds-base: 60
    cooldown-seconds-reduction-per-tier: 6

  ward:
    enabled: true
    radius: 6.0
    damage-base: 5.0
    damage-per-tier: 0.7
    knockback-strength: 1.4
    resistance-duration-seconds: 6
    resistance-amplifier: 2
    cooldown-seconds-base: 45
    cooldown-seconds-reduction-per-tier: 5

  eye:
    enabled: true
    minion-count-base: 1
    minion-lifespan-seconds: 60
    cooldown-seconds-base: 90
    cooldown-seconds-reduction-per-tier: 9

  tide:
    enabled: true
    surge-speed: 1.8
    surge-speed-per-tier: 0.1
    damage-base: 4.0
    damage-per-tier: 0.6
    knockback-strength: 1.4
    cooldown-seconds-base: 45
    cooldown-seconds-reduction-per-tier: 4

  snout:
    enabled: true
    minion-count-base: 2
    minion-lifespan-seconds: 90
    cooldown-seconds-base: 120
    cooldown-seconds-reduction-per-tier: 12

  rib:
    enabled: true
    minion-count-base: 2
    minion-lifespan-seconds: 60
    shell-duration-seconds: 6
    cooldown-seconds-base: 60
    cooldown-seconds-reduction-per-tier: 6

  spire:
    enabled: true
    dash-speed: 2.0
    dash-damage-base: 4.0
    dash-damage-per-tier: 0.6
    damage-amplification: 0.6
    cooldown-seconds-base: 30
    cooldown-seconds-reduction-per-tier: 3

  wayfinder:
    enabled: true
    lunge-range: 20.0
    lunge-speed: 1.6
    lunge-speed-per-tier: 0.1
    cooldown-seconds-base: 25
    cooldown-seconds-reduction-per-tier: 2

  shaper:
    enabled: true
    haste-amplifier-per-tier: 1
    radius: 6.0
    damage-base: 5.0
    damage-per-tier: 0.7
    cooldown-seconds-base: 30
    cooldown-seconds-reduction-per-tier: 3

  silence:
    enabled: true
    radius: 15.0
    potion-duration-seconds: 20
    pearl-disable-seconds: 10
    cooldown-seconds-base: 90
    cooldown-seconds-reduction-per-tier: 8
    echo-health-threshold: 4.0
    echo-cooldown-seconds: 120
    echo-knockback-strength: 1.3
    echo-resistance-regen-seconds: 15
    echo-weakness-seconds: 5

  raiser:
    enabled: true
    entity-pull-radius: 15.0
    debuff-duration-seconds: 4
    pearl-cooldown-seconds: 10
    cooldown-seconds-base: 120
    cooldown-seconds-reduction-per-tier: 12

  host:
    enabled: true
    radius: 10.0
    health-steal-base: 4.0
    health-steal-per-tier: 0.5
    cooldown-seconds-base: 120
    cooldown-seconds-reduction-per-tier: 12

  flow:
    enabled: true
    duration-seconds: 20
    heart-cost-interval-seconds: 1
    heart-cost-amount: 2.0
    burst-radius: 5.0
    burst-damage-base: 3.0
    burst-damage-per-tier: 0.5
    cooldown-seconds-base: 60
    cooldown-seconds-reduction-per-tier: 6

  bolt:
    enabled: true
    target-range: 20.0
    chain-range: 10.0
    max-chains: 3
    initial-damage-base: 6.0
    initial-damage-per-tier: 0.6
    subsequent-damage-base: 4.0
    subsequent-damage-per-tier: 0.4
    weakness-duration-seconds: 5
    charged-hits: 3
    charged-hits-duration-seconds: 20
    charged-hit-damage-base: 2.0
    charged-hit-damage-per-tier: 0.3
    cooldown-seconds-base: 20
    cooldown-seconds-reduction-per-tier: 2
